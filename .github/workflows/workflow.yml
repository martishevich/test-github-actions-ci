name: i learn github actions
on: [push]
jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
      - name: install dependencies
          run: |
            npm install
      - name: Run tests
        run: |
          npm -s test